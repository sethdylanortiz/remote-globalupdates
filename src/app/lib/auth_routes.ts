const ROOT = "/login";
// todo, change this link: 
const DEFAULT_REDIRECT = "/";
const PUBLIC_ROUTES = "/login";

export { ROOT, DEFAULT_REDIRECT, PUBLIC_ROUTES };

/*

export const ROOT = '/';
export const PUBLIC_ROUTES = ['/'];
export const DEFAULT_REDIRECT = '/protected';

 */